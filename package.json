{
  "scripts": {
    "dev": "concurrently \"npm run frontend\" \"npm run backend\" \"npm run ml\"",
    "frontend": "cd frontend && npm run dev",
    "backend": "cd backend && npm run dev",
    "ml": "cd ml_service && uv run app.py"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}